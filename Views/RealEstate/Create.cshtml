@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutPageHome.cshtml";
}
<link rel="stylesheet" href="~/Font-end/CSS/RealEstate.css" />
<script defer src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script defer src="~/Scripts/Scripts-fontend/API-Address-Selector.js"></script>
<div class="listing-form">
    <h2>Tạo tin đăng</h2>

    <form method="post" action="@Url.Action("Create", "RealEstate")">

        <!-- Loại giao dịch -->
        @*<div class="form-group">
            <label>Loại giao dịch:</label>
            <div class="radio-button">
                <input type="radio" name="TransactionType" value="Bán" checked>
                <label>Bán</label>

                <input type="radio" name="TransactionType" value="Mua">
                <label>Mua</label>
            </div>
        </div>*@

        <!-- Loại hình BĐS -->
        <div class="form-group">
            <label>Loại hình bất động sản:</label>
            <select id="propertyType" name="Type">
                <option value="">-- Chọn loại hình --</option>
                <option>Căn hộ</option>
                <option>Nhà phố</option>
                <option>Đất nền</option>
            </select>
        </div>

        <!-- Diện tích -->
        <div class="form-group">
            <label>Diện tích (m²):</label>
            <input type="number" id="area" name="Area" placeholder="Nhập diện tích">
        </div>

        <!-- Giấy tờ -->
        <div class="form-group">
            <label>Giấy tờ hợp pháp:</label>
            <select id="legal" name="Legal">
                <option value="">-- Chọn loại giấy tờ --</option>
                <option>Sổ đỏ</option>
                <option>Sổ hồng</option>
                <option>Giấy tay</option>
            </select>
        </div>

        <!-- Địa chỉ -->
        <div class="form-group">
            <label>Thành phố / Quận / Phường:</label>
            <div class="address-select">

                <select id="city" name="City">
                    <option value="">-- Chọn Tỉnh/Thành phố --</option>
                </select>

                <select id="district" name="District">
                    <option value="">-- Chọn Quận/Huyện --</option>
                </select>

                <select id="ward" name="Ward">
                    <option value="">-- Chọn Phường/Xã --</option>
                </select>

            </div>
        </div>

        <!-- Địa chỉ cụ thể -->
        <div class="form-group">
            <label for="specificAddress">Địa chỉ cụ thể:</label>
            <input type="text" id="specificAddress" name="Address" placeholder="Nhập địa chỉ cụ thể">
        </div>

        <!-- Giá -->
        <div class="form-group">
            <label for="price">Báo giá (VNĐ):</label>
            <input type="number" id="price" name="Price" placeholder="Nhập giá">
        </div>

        <!-- Mô tả -->
        <div class="form-group">
            <label>Mô tả:</label>
            <textarea id="description" name="Description" rows="4" placeholder="Nhập mô tả chi tiết..."></textarea>
        </div>

        <!-- Đính tệp -->
        <div class="form-group">
            <label>Đính tệp:</label>
            <input type="file" id="file">
        </div>

        <!-- Nút gửi -->
        <div class="form-group">
            <button type="submit" class="submit-btn">Tiếp tục</button>
        </div>

    </form>
</div>
